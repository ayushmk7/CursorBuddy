# Superseded Test Plan

This historical plan is retained only to preserve planning history.

It is not the current source of truth for the product architecture.

## Current architecture reminder

```text
VS Code extension -> sidecar -> Go bridge -> OpenClaw service -> OpenAI Realtime
```

If this archived plan conflicts with the current top-level docs, the top-level docs win.
